This project is developed with react-native for Shopify Mobile Developer Challenge.

To run the project, you can type the following commands:

 `npm install`

 `npm start`

 To view the project on your device, you need to install Expo.